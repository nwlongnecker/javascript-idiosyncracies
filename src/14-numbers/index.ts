/*
 * All numbers in JavaScript are represented as floating point numbers. This means that JavaScript
 * has some trouble representing some floating point values, so you may not always get the results
 * you expect when doing floating point arithmetic.
 */

console.log(`0.1 + 0.2 = ${0.1 + 0.2}`);

// `yarn run numbers` to execute this file
